#include <iostream>
#include <algorithm>
#include <cstring>
#include <queue>
#include <unordered_map>
#include <unordered_set>
#include <map>
#include <set>
#include <vector>
#include <bitset>
#include <cmath>

using namespace std;
typedef long long ll;

int t, n, m, k;

int main()
{
    scanf("%d", &t);

    while (t--)
    {
        scanf("%d%d%d", &n, &m, &k);
        
        if (k - 1 < n - k) k = n - k + 1;

        
    }

    return 0;
}